{
    "version": 2,
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.py"
        },
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ],
    "framework": "vite",
    "installCommand": "npm ci",
    "buildCommand": "npm run build --workspace=flowjob-frontend",
    "outputDirectory": "frontend/dist"
}