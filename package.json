{
  "name": "flowjob",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"python3 -m uvicorn backend.main:app --reload --port 8000\" \"npm run dev --prefix frontend-react\"",
    "dev:frontend": "npm run dev --prefix frontend-react",
    "dev:backend": "python3 -m uvicorn backend.main:app --reload --port 8000"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
